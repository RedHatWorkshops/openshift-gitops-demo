apiVersion: v1
kind: ImageStream
metadata:
  labels:
    app: {{ .Values.application_name }}
  name: {{ .Values.application_name }}
spec:
  lookupPolicy:
    local: true
  tags:
  - from:
      kind: DockerImage
      name: {{ .Values.terminal_image }}
    name: latest
